import React from "react";

import { Container, Main, Footer, Cards } from "@components";

const Home: React.FC = () => {
    return (
        <Container>
            <Main />
            <Cards />
            <Footer />
        </Container>
    );
};

export default Home;
